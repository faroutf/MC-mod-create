# ===================================================
# Pick one random unique task for a slot (macro)
# Parameters: slot_num
# ===================================================
$function hard_survival_1:tasks/pick_reroll with {slot:$(slot_num)}
